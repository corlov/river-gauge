// Назначаем номер пина, к которому подключена управляющая схема.
// Использование константы делает код более читаемым и легким для изменения.
const int controlPin = 5;

// --- Функция setup() ---
// Этот код выполняется один раз при включении или перезагрузке Arduino.
void setup() {
  // Устанавливаем наш пин (номер 5) в режим ВЫХОДА (OUTPUT).
  // Это говорит Arduino, что мы будем подавать напряжение на этот пин, а не считывать его.
  pinMode(controlPin, OUTPUT);
  pinMode(17, OUTPUT);
}

// --- Функция loop() ---
// Этот код выполняется в бесконечном цикле после завершения setup().
void loop() {
  // 1. Включаем светодиод
  digitalWrite(controlPin, HIGH); // Подаем 5В на пин 5
  digitalWrite(17, HIGH);

  // 2. Ждем одну секунду (1000 миллисекунд)
  // Светодиод будет гореть в течение этого времени.
  delay(5000);

  // 3. Выключаем светодиод
  digitalWrite(controlPin, LOW);  // Подаем 0В (землю) на пин 5
  digitalWrite(17, LOW);
  // 4. Ждем еще одну секунду
  // Светодиод будет выключен в течение этого времени.
  delay(1000);

  // После этого цикл начинается заново с пункта 1.
}
